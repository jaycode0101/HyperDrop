# Optional: Self-hosted signaling server (for when PeerJS cloud is unavailable)
# Deploy to Railway, Render, or any Node.js host
VITE_SIGNALING_SERVER=

# Optional: Custom TURN server credentials
# Free public TURN servers are included by default
# For production, consider Metered (https://metered.ca) or self-hosted coturn
VITE_TURN_SERVER=
VITE_TURN_USERNAME=
VITE_TURN_CREDENTIAL=
